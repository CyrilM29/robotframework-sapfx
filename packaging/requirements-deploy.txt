# Dépendances runtime du pack de déploiement (le wheel apporte robotframework ;
# on ré-épingle ici ce que l'environnement cible doit fixer exactement).
# pywin32 épinglé exact (convention 6 du dépôt) : #1 source de casse COM.
pywin32==311; platform_system == "Windows"
# Côté Fiori / web. Après installation : `rfbrowser init chromium` (option
# -WithBrowsers de install.ps1) ou utiliser le Chrome système (channel=chrome).
# Borne haute alignée sur l'extra `web` du wheel : la bibliothèque Fiori vise
# des chemins internes de Browser, une majeure se re-qualifie (le pack fige de
# toute façon la version exacte dans constraints-deploy.txt).
robotframework-browser>=20.0,<21
# Assertions visuelles et empreinte de la sentinelle de dérive
# (`Screen Should Match Baseline`, `Check Screen Against Watch`) : Pillow ne
# sert qu'à décoder le PNG à la frontière (extra `visual` du wheel).
Pillow>=10.0
# Canal RFC de SapApiLibrary (`Open Rfc Connection`/`Call Rfc`) : nécessite
# `pyrfc` + le runtime SAP NW RFC. Provisionnement scripté par `install-rfc.ps1`,
# procédure et pièges dans le README du pack (« Canal RFC ») : sur un poste
# porteur de SAP GUI 8.00 le SDK sous licence est souvent inutile (runtime déjà
# dans System32, mesuré le 2026-08-27), mais le venv doit être en Python
# 3.10-3.12, aucune roue `pyrfc` n'existant au-delà. Les keywords OData, eux,
# n'ont besoin de rien de tout cela.
