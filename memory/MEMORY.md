# Index mémoire du projet

Une ligne par fiche ; n'ouvrir une fiche que si sa description est pertinente.
Règles : [README.md](README.md).

- [Distribution, deux canaux](decision-pypi-canal-unique.md) : 2026-08-03, PyPI = libs seules, pack ZIP GitHub = SEULE install complète (mais PAS « milieu fermé » : l'installateur tire les dépendances de PyPI/miroir) ; le « full PyPI » (sapfx init, extras [all]) est phasé mais REPORTÉ
- [Hot-reload plugins rf-mcp](rfmcp-plugin-hot-reload.md) : reload de la couche plugin sans redémarrer le serveur ; routage page_source/application_state ; le diff des providers exige page_source_filtered=true ; **complément 2026-08-22** : si la bibliothèque importe le MODULE (et non la fonction), un simple `importlib.reload` du module pur suffit
- [A4H, écriture/suppression SE16](a4h-se16-write-delete.md) : SE16 fait Create/Change/Delete en place (DEVELOPER) ; Create depuis l'écran INITIAL ; SM30 KO sur SCARR ; séparateur de milliers dans les comptes
- [Run Transaction, tcodes paramétrés](run-transaction-tcodes-parametres.md) : faux négatif quand sy-tcode ≠ code saisi (tcodes IMG générés) : percevoir avant de conclure ; correspondance via CUS_IMGACH
- [Deux médaillons, suite et library](deux-medaillons-suite-library.md) : 2026-08-07, `logo.png` (projet complet) partout / `logo-library.png` (bibliothèques seules) sur PyPI ET les pages Libdoc publiées ; bascule fail-closed dans export_public_tree.py ET pypi-publish.yml ; gen_icons.py puis regen_recorder ; détourage à tolérance 60, pas 90
- [Tournage des vidéos de démo](tournage-video-demos.md) : gdigrab, fenêtres GPU noires (capturer une région), arrêt ffmpeg par « q » sinon tampon perdu, bornage AVANT -i quand setpts accélère, couper la fin de prise (bureau visible)
- [Table control, lignes réservées](table-control-lignes-reservees.md) : `RowCount` compte les lignes RÉSERVÉES (47 annoncées, 26 remplies) et la scrollbar plafonne à `total - visible` ; lire au-delà lève un com_error, la dernière fenêtre chevauche la précédente
- [Libdoc grave des chemins absolus](libdoc-chemins-absolus.md) : chaque champ `source` (bibliothèque ET keywords) porte le chemin du poste de génération : relativiser en `src/…` dans la spec avant de reconstruire les pages publiées, sinon l'export public s'arrête le jour de la release
- [Assertion Fiori, ni trop tôt ni localisée](assertion-fiori-ni-trop-tot-ni-localisee.md) : le rendu n'est pas la donnée (`Wait For Ui5 Idle` attend les requêtes DÉJÀ parties, pas un premier rendu) et l'app suit la locale du navigateur (« Lancer » pour « Go ») : viser les ids FE stables
- [UIA / desktop générique](uia-desktop-generique-composition.md) : 2026-08-12, rien d'UIA à intégrer dans SAPFX (Scripting API supérieure sur SAP GUI) ; appli Windows non-SAP d'un flux = composer PlatynUI/FlaUI à côté ; idée en réserve : repli UIA avant coordonnées sur GuiShell opaque, seulement sur cas réel
- [SE16, champs omis de l'écran de sélection](se16-ecran-selection-champs-omis.md) : 2026-08-17, le champ client et les champs FLTP sont retirés en silence (SAPLANE : 14 critères pour 18 champs) et une table large dépend d'un choix persistant par utilisateur (DD02L : 3 clés seulement) ; DD03L est la source du vrai ordre
- [SE16, sélection multiple d'une liste](se16-selection-multiple-liste-noms.md) : 2026-08-17, ids de la modale « Multiple Selection » (bouton VALU_PUSH, table control SAPLALDBSINGLE, reprise btn[8]) pour traiter un lot de noms sans préfixe commun en un passage ; non rémanente
- [SE16, écran de sélection généré au premier accès](se16-ecran-selection-genere-premier-acces.md) : 2026-08-17, `/1BCDWB/DB<table>` généré à la volée (~1 s, busy terminé ≠ écran affiché : attendre txtMAX_SEL) et la génération peut émettre un dialogue modal d'information (SGEOCITY/FLTP, SAPMSDYP) sans statut type E ; détection structurelle MESSTXT, verdict `dialog` du balayage ; **fiche corrigée le 2026-08-18** : la logique vit dans UN keyword de bibliothèque (`Reach Se16 Selection Screen`), plus dans trois copies, et le verdict `dialog` doit être BORNÉ pour rester un signal
- [Recorder web, sélecteurs en sous-chaîne sûre](recorder-selecteurs-sous-chaine-sure.md) : 2026-08-17 **corrigée le 2026-08-18**, les textes de SÉLECTEUR s'assainissent plutôt que de s'échapper, MAIS tronquer peut recibler en silence (choisir le guillemet du littéral au lieu de couper), assainir d'un côté sans normaliser de l'autre ne résout rien (le moteur role comparait la valeur brute), et une dégradation doit être ANNONCÉE dans la ligne émise
- [Condition Robot et texte SAP](robot-if-texte-sap-apostrophe.md) : 2026-08-18, `IF '${msg}' != '${EMPTY}'` casse en SyntaxError dès qu'un message SAP contient une apostrophe (locale FR) : utiliser la forme EXPRESSION `$msg` / `$state["verdict"]` pour toute valeur venue du système testé
- [Replay du recorder, vert et faux](replay-recorder-vert-et-faux.md) : 2026-08-19, `--replay` sortait en 0 après avoir IGNORÉ tous les steps (cas de la suite resource-first : 100 % de keywords métier) ; un step non rejoué doit être bloquant, et le séparateur de cellules RF n'est pas « 4 espaces » mais « tabulation ou 2 espaces et plus »
- [Sortie console des gardes, UTF-8 sur les deux flux](gardes-sortie-console-utf8.md) : 2026-08-19, sous Windows `stdout` est en erreurs strictes (une flèche TUE le processus) et `stderr` en `backslashreplace` (mojibake silencieux) : un garde relayé arrivait abîmé et la branche informative du hook mourait sur son propre message ; bascule dans `scripts/_common.py`, `encoding` explicite à la lecture d'un sous-processus, copie assumée pour les scripts embarqués dans le pack
- [Arrêter un recorder de l'extérieur](arret-externe-recorder-teardown.md) : 2026-08-19, `terminate()` saute le `finally` (OK-code en attente perdu, `Session.Record` laissé ARMÉ côté SAP GUI : F4 modal, drag & drop désactivé) : sentinelle fichier `--stop-file` sondée par la boucle, kill seulement en dernier recours
- [Deux canaux de distribution, une seule frontière](deux-canaux-distribution-une-frontiere.md) : 2026-08-19, l'export public et le pack Windows filtraient le privé chacun de leur côté, et un seul le faisait (copier un dossier PARENT a livré du contenu exclu nommément par l'autre canal, pendant une release entière) ; liste blanche plutôt que liste noire, LA liste de l'export portée par les deux canaux, un fichier livré ne pilote jamais un script absent, et ne jamais renommer le secret dans la documentation de sa fuite (le scan anti-fuite en interdit le nom, y compris dans les tests)
- [rf-mcp remplace un retour `None` par « OK »](rfmcp-retour-none-devient-ok.md) : 2026-08-19, `output = str(result) if result is not None else "OK"` rendait le garde « sortie vide » du pont contexte-RF inatteignable, et une section d'état absente arrivait servie comme un franc « OK » ; le test qui la couvrait assertait une forme que rf-mcp n'émet jamais
- [Trois gardes verts qui ne pouvaient pas échouer](gardes-qui-ne-peuvent-pas-echouer.md) : 2026-08-19, `hasattr` sur une sous-classe de `Protocol` vaut toujours vrai, un anti-duplication qui cherche un NOM ne voit pas la copie inline, une doublure posée dans le mauvais espace de noms laisse passer le vrai appel (indice : 1,53 s contre 0,04 s) ; écrire la contre-épreuve avec le garde
- [Une lecture d'état ne modifie pas la page](lecture-d-etat-ne-modifie-pas-la-page.md) : 2026-08-19, l'état Fiori appelait un keyword qui échoue hors runtime UI5 (erreur permanente sur les cibles wc/sid/dom) et réinstallait le bundle, donc instrumentait `fetch`/XHR de l'app pour la simple regarder ; sonde dédiée + « sans objet » distingué de « échoué »
- [Canal API, les pièges d'une cible OData v4](canal-api-odata-v4-pieges.md) : 2026-08-19, quatre comportements qu'aucune Gateway SAP v2 ne montre (auth simulée sur le service, entité draft-enabled dont le brouillon n'entre pas dans le comptage, `Location` annoncé mais non adressable, espace encodé en « + » REFUSÉ) ; une cible v4 est ce qui révèle les défauts qu'une Gateway indulgente absorbe
- [Un canal testé seulement en indirect](angle-mort-canal-teste-en-indirect.md) : 2026-08-19, 32 keywords couverts en live par un seul keyword de lecture, un test unitaire qui GRAVAIT l'encodage fautif, et le seul usage live capable de l'attraper qui échouait plus tôt pour une autre raison ; trois couches de masquage, et une couverture en pourcentage qui reste excellente pendant ce temps
- [Une annotation OData déclarée n'est pas permissive](annotation-odata-declaree-non-permissive.md) : 2026-08-22, garde de capacités d'écriture écrite fausse DEUX fois (d'abord au niveau du service, muet sur 4 entity sets sur 16 ; puis en comptant la PRÉSENCE de l'annotation et non sa valeur, `creatable="false"` valant appui) ; le critère juste croise autorisé ET déclaré, et fait tomber les candidats de 2 à 0
- [Une table lisible n'est pas une table écrivable](table-lisible-nest-pas-table-ecrivable.md) : 2026-08-22, une cible d'écriture retenue sur 4 critères (observable par l'autre canal, structure minimale, table de démo, clé courte) refuse la création en type `E` ; le 5e critère, invisible depuis l'API, est l'indicateur de maintenance du dictionnaire, vide sur tout un modèle de démonstration ; sonder la maintenabilité AVANT de concevoir le cycle, et faire de la cible un paramètre
- [La correspondance entity set / table ne se déduit pas](correspondance-entity-set-table-non-deductible.md) : 2026-08-22, le `$metadata` ne nomme jamais la table source (donnée de configuration, chaque couple prouvé) ; une table homonyme SAINE mais VIDE aurait produit un échec faux ; trois inégalités légitimes (draft-enabled, projection filtrée, périmètre utilisateur) et le mandant identique exigé des deux canaux
- [Baselines visuelles liées à la résolution](baselines-visuelles-liees-a-la-resolution.md) : 2026-08-19, une empreinte perceptuelle encode la GÉOMÉTRIE de capture autant que le contenu (1920x1032 contre 4676x2454 : Hamming 8 > seuil 5 sur des écrans strictement identiques, dérive visuelle SEULE, aucune dérive structurelle) ; décision prise de ne PAS régénérer, comparer d'abord les dimensions du `.actual.png` avant de conclure à une régression ; depuis le 2026-08-20 l'outil répond par `per_resolution=True` : une baseline par géométrie côté assertions, et côté sentinelle des références VISUELLES par géométrie avec le canal structurel partagé
- [Un en-tête HTTP posé par urllib ne garde pas sa casse](entetes-http-urllib-casse-normalisee.md) : 2026-08-22, `APIKey` part en `Apikey` (normalisation `capitalize()` de `Request.add_header`, sans conséquence côté serveur) et Python 3.14 a rendu `Request.get_header` EXACT, donc deux tests neufs rouges sur un en-tête pourtant bien envoyé ; les en-têtes déjà en forme `capitalize()` comme `Authorization` masquaient le piège ; asserter la propriété observable (en-tête envoyé, casse indifférente), jamais la représentation intermédiaire
- [Un serveur peut compresser une réponse qu'on ne lui a pas demandé de compresser](reponse-compressee-sans-l-avoir-demandee.md) : 2026-08-22, le bac à sable api.sap.com sert son `$metadata` en `Content-Encoding: gzip` sans qu'aucun `Accept-Encoding` ait été envoyé, et le parseur XML accusait le document (« invalid token: line 1, column 0 », la signature d'un corps binaire) alors que le fautif était le transport ; les lectures JSON passaient (ce serveur ne comprime que la voie XML), donc deux cibles live sur trois ne pouvaient pas voir le trou ; devant un 200 au corps illisible, lire les en-têtes de réponse AVANT le contenu
- [Sur ABAP 2023, la grille ALV n'est plus au bout du chemin de shell habituel](alv-enveloppee-dans-un-splitter-en-2023.md) : 2026-08-23, `cntlGRID1/shellcont/shell` désigne un `GuiSplitterShell` et non la grille ; profondeur VARIABLE (1 niveau en SE16, 2 en SM50 qui ajoute un panneau HTML), donc aucun suffixe fixe à concaténer ; touche toutes les lectures de grille de la couche resource d'un coup, et déguise un localisateur faux en diagnostic métier faux (les données de démo étaient présentes, la vérification passait par une grille)
- [Le premier appel OData d'un système froid ressemble à une panne de réseau](premier-appel-odata-systeme-froid.md) : 2026-08-23, préflight rendant `unreachable / timed out` sur une Gateway parfaitement active (2,2 s au 1er appel, 0,1 s au 2e, 0 s au 3e), puis suite API rouge sur le premier `$count` d'un service jamais sollicité ; le chargement se paie une fois PAR SERVICE, et la remédiation affichée oriente vers la connectivité, seul endroit où il n'y a rien à trouver ; rejouer avant de conclure. NB : la Gateway de l'image ABAP 2023 est active d'origine, aucune activité IMG requise
- [Une clé de nœud d'arbre SAP GUI est complétée à gauche](cles-de-noeud-completees-a-gauche.md) : 2026-08-23, `'         11'` et non `'11'` sur un `TableTreeControl` (SICF) ; une clé « nettoyée » échoue par un `com_error` sans message, ce qui fait suspecter la méthode au lieu de la valeur ; journaliser les identifiants opaques avec `repr()` et les repasser tels quels ; second piège du même écran, trois nœuds nommés `webgui` cohabitent, donc identifier par le chemin reconstruit (`GetSubNodesCol`, `GetParent` n'existant pas) et jamais par le numéro
- [Le texte rendu d'un contrôle n'est pas sa valeur](lire-la-propriete-plutot-que-le-rendu.md) : 2026-08-24, le rendu ajoute ce que le contrôle dessine (compteur : « Accessories\n34 » pour un titre « Accessories ») et un contrôle rendu mais MASQUÉ n'a pas de texte lisible (attente de 10 s qui ressemble à une dérive de localisateur) ; lire la PROPRIÉTÉ au registre (`Get Ui5 Property/Properties`), garder `Get Ui5 Text` pour « ce que l'utilisateur voit »
- [Cliquer une ligne déjà sélectionnée ne navigue pas](cliquer-une-ligne-deja-selectionnee-ne-navigue-pas.md) : 2026-08-24, une `sap.m.List` en mode sélection n'émet rien quand la sélection ne CHANGE pas, le clic « réussit » sans naviguer et l'échec tombe deux étapes plus loin sur un bouton « invisible » innocent ; viser une ligne non sélectionnée ET faire constater son arrivée au mot-clé de navigation
- [L'iframe d'application d'un launchpad n'a pas d'identifiant stable](iframe-application-launchpad-id-genere.md) : 2026-08-23, premier pilotage d'un VRAI cFLP (Work Zone, nouvelle barre shell) ; `__container1` puis `__container4` puis `__container3` pour la même app, et la docstring donnait `iframe[id*="application"]` qui ne correspond à rien ; `Get Ui5 App Frame` / `Push Ui5 App Frame` désignent la frame par ce qu'elle EST ; annexes du même run : la nouvelle barre shell est en Web Components, et l'iframe naît APRÈS le retour de `Open App By Intent`
- [Une seule release ne montre pas ses hypothèses](une-seule-release-ne-montre-pas-ses-hypotheses.md) : 2026-08-24, une campagne de launchpad verte 19/19 sur SAPUI5 1.71 portait six hypothèses invisibles, révélées en la portant sur 1.120 (ancre qui change de nom ET de type, position d'acquittement qui COPIE au lieu de fermer, sonde de tuiles rendant du vide SANS échouer, seuils gravés, budgets d'attente, contrôle disparu) ; ce qui diverge devient une variable ou une stratégie nommée, jamais un `IF` sur la version, et la porte de sortie est le REJEU live de l'ancienne suite
- [Un dièse en tête de valeur Robot est un commentaire](diese-en-tete-de-cellule-robot-est-un-commentaire.md) : 2026-08-24, `${LOGIN_FORM}    #LOGIN_FORM` définit une variable VIDE, le dry run passe (une chaîne vide reste une chaîne) et le run live échoue en cascade sur 19 tests avec un message qui ressemble à une dérive de localisateur ; écrire `[id="LOGIN_FORM"]`, et lire `{'css': ''}` dans la première ligne d'échec. Second enseignement du même run : le budget de boot (180 s) appliqué à TOUTES les opérations d'élément rend une campagne rouge illisible, séparer les deux budgets
- [Un code injecté dans une page y reste, même après avoir été corrigé](bundle-injecte-garde-a-vie-par-la-page.md) : 2026-08-24, le bundle JS de la bibliothèque web était gardé par un « déjà présent », donc la première version reçue par une page y restait à vie ; après un échange à chaud de la bibliothèque, les nouveaux mots-clés sont visibles côté Robot et l'appel sort en « ... is not a function », message qui accuse le mot-clé là où le fautif est le cache d'injection ; versionner par le contenu, et surtout porter l'état des HOOKS sur la fenêtre plus marquer chaque enveloppe (les reposer double le comptage réseau, les abandonner rend une page toujours « au repos », ce qui est pire) ; la mesure qui tranche se lit de façon SYNCHRONE, juste après l'émission d'une requête
- [La remasterisation des 500 lignes](remasterisation-500-lignes.md) : découper 23 fichiers (~22 000 lignes) sans changer une surface : assemblage JS à l'octet près, monkeypatch au site d'appel, re-exports protégés de ruff --fix, mixins typés via un socle commun
- [Piloter les recorders depuis un test](piloter-les-recorders-dans-un-test.md) : 2026-08-25, validation LIVE des deux recorders (bureau contre ECC réel, web contre une application Fiori Elements réelle) ; les deux marchent, et les quatre échecs venaient du harnais : sortie de sous-processus bloc-tamponnée dans un tube (lancer en `-u`), saisie web transcrite seulement à la PERTE DE FOCUS (c'est `change` qui la porte), bouton d'export portant transitoirement son retour visuel « copied », et paire resource-first espacée de 350 ms dont deux attentes armées trop tôt captent le même fichier
- [Rien ne relie un run à un livrable](rien-ne-relie-un-run-a-un-livrable.md) : 2026-08-25, inventaire de la couche restitution : `output.xml` jamais lu (aucun `ExecutionResult`), quatre rapports Markdown enfouis dans le `log.html` par `Log`, documents ISTQB statiques et déconnectés des résultats, traçabilité rédigée en prose par un LLM au lieu d'être calculée ; les propriétés techniques (déterminisme, autoportance, opt-in) sont déjà là, il leur manque d'être servies à un lecteur non testeur
